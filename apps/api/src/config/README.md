# Config

This folder contains backend configuration and environment handling.

## Purpose
- load and validate environment variables
- centralize configuration for database, auth, queues, and services
- keep configuration concerns separate from domain logic

## What belongs here
- environment config modules
- validation schemas
- app-wide configuration providers
