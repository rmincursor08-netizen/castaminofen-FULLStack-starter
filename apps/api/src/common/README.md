# Common

This folder contains cross-module conventions and reusable base abstractions.

## Purpose
- define shared conventions for the backend architecture
- provide base classes, interfaces, and types used across layers
- reduce inconsistency between modules

## What belongs here
- common response types
- common pagination abstractions
- base entities and DTO patterns
- common decorators and constants
