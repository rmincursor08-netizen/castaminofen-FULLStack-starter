# config

This folder contains application configuration and environment settings.

## Purpose
- centralize app-wide configuration values
- keep environment-based settings explicit and typed
- support routing, feature flags, storage keys, and API base URLs
