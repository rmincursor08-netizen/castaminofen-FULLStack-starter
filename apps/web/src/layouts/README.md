# layouts

This folder contains layout-level compositions used across the app.

## Purpose
- define page shells, navigation containers, and reusable route layouts
- keep layout structure separate from feature content
- support consistent page composition across the app
