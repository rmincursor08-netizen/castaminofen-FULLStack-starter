# providers

This folder contains application-level providers.

## Purpose
- register context providers for state, theme, localization, and data fetching
- keep provider wiring separate from page and feature code
- make global dependencies configurable and composable
