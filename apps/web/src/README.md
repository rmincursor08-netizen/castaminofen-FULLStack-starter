# Next.js App Router Architecture

This folder contains a Feature-Sliced Design-oriented frontend architecture for the web app.

## Architectural principles
- business logic is separated from UI
- UI is composed from features, widgets, shared primitives, and layouts
- routing, state, API, and theming are organized as explicit layers
- the app is ready for App Router, server/client boundaries, and future scaling
