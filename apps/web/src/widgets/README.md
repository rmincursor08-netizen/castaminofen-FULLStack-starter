# widgets

This folder contains composite UI blocks that combine features and shared primitives into larger interface units.

## Purpose
- host larger reusable UI sections such as headers, sidebars, player bars, and dashboards
- compose low-level UI into meaningful experience blocks
- keep composition logic separate from feature implementation
