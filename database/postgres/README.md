# PostgreSQL Database Design

This directory contains a normalized, scalable PostgreSQL schema blueprint for a content platform.

## Goals
- support users, creators, content, media, search, analytics, recommendations, and notifications
- keep schema normalized and extensible
- prepare for future partitioning, archiving, and sharding
- map cleanly to Prisma models and Postgres best practices
