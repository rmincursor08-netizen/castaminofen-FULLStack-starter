# Docker

This folder contains container definitions and local environment packaging assets.

## Purpose
- provides reproducible container builds for services and dependencies
- simplifies local development and deployment workflows
- standardizes runtime environments across machines

## What belongs here
- Dockerfiles
- compose files
- container build helpers

## Design rule
Containers should be deterministic, minimal, and aligned with production behavior.
